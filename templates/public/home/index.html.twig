{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <section class="relative isolate overflow-hidden ">
        <div>
            <video class="absolute inset-0 -z-20 h-full w-full object-cover" autoplay loop muted playsinline>
                {% if 'Firefox' in app.request.headers.get('User-Agent') %}
                    <source src="{{ asset('media/video/bg-home.webm') }}" type="video/mp4">
                {% else %}
                    <source src="{{ asset('media/video/bg-home.mp4') }}" type="video/mp4">
                {% endif %}
                Votre navigateur ne prend pas en charge les vidéos.
            </video>
            <div class="absolute top-0 left-0 w-full h-full bg-gray-900 opacity-60 -z-10"></div>
        </div>
        <div class="mx-auto max-w-2xl py-32 sm:py-48 lg:py-56 z-20">
            <div class="hidden sm:mb-8 sm:flex sm:justify-center">
                <div class="relative rounded-full px-3 py-1 text-sm leading-6 text-gray-400 ring-1 ring-white/10 hover:ring-white/20">
                    Announcing our next round of funding. <a href="#" class="font-semibold text-white"><span class="absolute inset-0" aria-hidden="true"></span>Read more <span aria-hidden="true">&rarr;</span></a>
                </div>
            </div>
            <div class="mt-8 sm:mt-0 text-center">
                <h1 class="text-balance text-4xl font-bold tracking-normal text-white sm:text-6xl">Assureur en Suisse pour les entreprises.</h1>
                <p class="mt-6 text-lg leading-8 text-gray-300 px-5">Anim aute id magna aliqua ad ad non deserunt sunt. Qui irure qui lorem cupidatat commodo. Elit sunt amet fugiat veniam occaecat fugiat aliqua.</p>
                <div class="mt-10 flex items-center justify-center">
                    <a href="#" class="rounded-full bg-primary px-4 py-4 text-sm font-semibold text-white shadow-sm hover:bg-primary-light gap-1.5">
                        Contacter un conseiller
                    </a>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
